import { ResourceDescriptor } from '../types';

export const descriptor: ResourceDescriptor = {
  name: 'flag',
  displayName: 'Flag',
  model: 'Flags',
  operations: ['getAll', 'get'],
  fields: [],
};
